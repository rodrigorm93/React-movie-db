//va tener todosa los types de nuestro objeto

export const types = {
  //hay que definir el string con el cual quiero trabajar el type en el reducer

  login: "[auth] login",
  logout: "[auth] logout",
};
